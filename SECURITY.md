# 安全边界

`mysql-wire-cpp` 当前是协议学习与数据库前端原型，不是面向生产环境的完整 MySQL Server。

## 当前不提供的安全能力

- 不验证用户名、密码或 auth response；
- 没有账号、角色和权限系统；
- 不支持 TLS，网络内容以明文传输；
- 没有连接数、请求速率或 SQL 资源限制；
- 每个连接创建一个 detached 工作线程；
- 没有完整的优雅停机和 session 回收协调机制。

任何能连接监听端口的客户端都可以调用后端 `SqlExecutor`。因此 demo 默认绑定
`127.0.0.1`，请勿将当前实现监听在公网地址，也不要把它接到包含敏感数据的后端。

## 使用要求

- 只在本机、容器内部或受信任的隔离网络运行；
- 使用防火墙限制监听端口；
- 不在握手、SQL 日志或 issue 中提交真实凭证和敏感数据；
- 面向不可信客户端前，先补齐认证、TLS、连接治理和后端授权。

安全问题报告方式将在公开 GitHub 仓库启用 private vulnerability reporting 后补充。
